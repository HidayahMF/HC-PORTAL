import { NavLink, Outlet, useNavigate } from 'react-router-dom'; import { FilePlus2, LayoutDashboard, LogOut, Users } from 'lucide-react'; import { useEffect,useState } from 'react'; import { api } from '../api'; import logo from '../assets/logobmcbg1.png';
export function Layout(){const nav=useNavigate();const[role,setRole]=useState('');useEffect(()=>{api.me().then(x=>setRole(x.user.role)).catch(()=>undefined)},[]);const logout=async()=>{await api.logout().catch(()=>undefined);nav('/kontrak/login',{replace:true})};return <div className="min-h-screen"><header className="border-b border-bmc-border bg-white"><div className="mx-auto flex max-w-7xl flex-wrap items-center justify-between gap-2 px-5 py-3 lg:px-8"><div className="flex items-center gap-3"><img src={logo} alt="Braja Mukti Cakra" className="h-9 w-auto max-w-[190px] object-contain"/><span className="border-l border-bmc-border pl-3 text-sm font-semibold text-bmc-primary">Kontrak Karyawan</span></div><nav className="flex flex-wrap items-center gap-1"><NavLink to="/kontrak/dashboard" className="btn-secondary border-0 px-3 py-2"><LayoutDashboard size={16}/>Dashboard</NavLink><NavLink to="/kontrak/contracts/new" className="btn-primary"><FilePlus2 size={16}/>Input Kontrak</NavLink>{role==='ADMIN'&&<NavLink to="/kontrak/admin/users" className="btn-secondary border-0 px-3 py-2"><Users size={16}/>Administrasi</NavLink>}<button onClick={logout} className="btn-secondary border-0 px-3 py-2"><LogOut size={16}/>Keluar</button></nav></div></header><main className="mx-auto max-w-7xl px-5 py-7 lg:px-8"><Outlet/></main></div>}
