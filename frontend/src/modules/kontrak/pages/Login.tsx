import { useNavigate } from 'react-router-dom'; import { ModuleLogin } from '../../../components/ModuleLogin'; import { request } from '../../../services/api'; export default function Login(){const navigate=useNavigate();return <ModuleLogin title="Kontrak Karyawan" fields={[{name:'nip',label:'NIP',placeholder:'Masukkan NIP'},{name:'birthCode',label:'Kode Tanggal Lahir',placeholder:'DDMMYYYY'}]} onSubmit={async values=>{await request('/kontrak/auth/login',{method:'POST',body:JSON.stringify(values)});navigate('/kontrak')}}/>}
