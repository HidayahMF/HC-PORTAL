import { useNavigate } from 'react-router-dom'; import { ModuleLogin } from '../../../components/ModuleLogin'; import { request } from '../../../services/api'; export default function Login(){const navigate=useNavigate();return <ModuleLogin title="Nomor Surat" fields={[{name:'nip',label:'NIP',placeholder:'Masukkan NIP'},{name:'birthDate',label:'Tanggal Lahir',type:'date',placeholder:''}]} onSubmit={async values=>{await request('/nomor-surat/auth/login',{method:'POST',body:JSON.stringify(values)});navigate('/nomor-surat')}}/>}
