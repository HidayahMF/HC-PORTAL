import { FormEvent, useState } from 'react'; import { LogIn } from 'lucide-react'; import logo from '../assets/logobmcbg1.png'; import { api } from '../api'; import { Spinner } from '../components/States';
export function Login({ onLogin }: { onLogin: () => void }) { const [nip,setNip]=useState(''); const [birthDate,setBirthDate]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); const submit=async(e:FormEvent)=>{e.preventDefault();setError('');setLoading(true);try{await api.login({nip,birthDate});onLogin()}catch(e){setError(e instanceof Error?e.message:'NIP atau tanggal lahir tidak valid.')}finally{setLoading(false)}}; return <main className="flex min-h-screen items-center justify-center bg-bmc-canvas px-5 py-8"><section className="w-full max-w-md border border-bmc-border bg-white p-7 shadow-sm sm:p-9"><div className="mb-8 text-center"><img src={logo} alt="Braja Mukti Cakra" className="mx-auto h-12 w-auto max-w-[260px] object-contain"/><div className="mx-auto mt-5 h-px w-10 bg-bmc-gold"/><h1 className="mt-5 text-xl font-semibold text-bmc-navy">Login Nomor Surat</h1><p className="mt-1 text-sm text-bmc-muted">Masuk menggunakan data employee HRIS.</p></div>{error&&<div className="mb-5 border border-red-200 bg-red-50 px-3 py-2.5 text-sm text-bmc-error">{error}</div>}<form onSubmit={submit} className="space-y-5"><label className="block"><span className="mb-2 block text-sm font-semibold">NIP</span><input required value={nip} onChange={e=>setNip(e.target.value)} maxLength={50} autoComplete="username" className="field" placeholder="Masukkan NIP"/></label><label className="block"><span className="mb-2 block text-sm font-semibold">Tanggal Lahir</span><input required type="date" value={birthDate} onChange={e=>setBirthDate(e.target.value)} autoComplete="bday" className="field"/></label><button disabled={loading} className="btn-primary w-full">{loading&&<Spinner/>}<LogIn size={16}/>{loading?'Memproses...':'Masuk'}</button></form></section></main> }
