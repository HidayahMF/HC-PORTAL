import { FormEvent, useState } from 'react'; import { LogIn } from 'lucide-react';
export function ModuleLogin({title,fields,onSubmit}:{title:string;fields:{name:string;label:string;type?:string;placeholder:string}[];onSubmit:(values:Record<string,string>)=>Promise<void>}){const [values,setValues]=useState<Record<string,string>>({});const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await onSubmit(values)}catch(e){setError(e instanceof Error?e.message:'Login gagal.')}finally{setBusy(false)}};return <main className="flex min-h-[70vh] items-center justify-center"><section className="w-full max-w-md border border-bmc-border bg-white p-8 shadow-sm"><div className="mb-8 text-center"><div className="mx-auto flex h-12 w-12 items-center justify-center rounded-xl bg-bmc-primary text-xl font-black text-white">B</div><h1 className="mt-5 text-xl font-bold text-bmc-primary">Login {title}</h1><p className="mt-1 text-sm text-bmc-muted">Gunakan akun dan hak akses modul ini.</p></div>{error&&<div className="mb-5 border border-red-200 bg-red-50 p-3 text-sm text-bmc-error">{error}</div>}<form onSubmit={submit} className="space-y-5">{fields.map(field=><label className="block" key={field.name}><span className="mb-2 block text-sm font-semibold">{field.label}</span><input required type={field.type||'text'} className="field" placeholder={field.placeholder} value={values[field.name]||''} onChange={e=>setValues({...values,[field.name]:e.target.value})}/></label>)}<button disabled={busy} className="btn-primary w-full"><LogIn size={16}/>{busy?'Memproses...':'Masuk'}</button></form></section></main>}
