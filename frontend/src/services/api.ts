export async function request<T>(path:string, options:RequestInit={}, base='/api'){const response=await fetch(`${base}${path}`,{credentials:'include',headers:{'Content-Type':'application/json',...(options.headers||{})},...options});const raw=await response.text();let body:{data?:T;message?:string}={};try{body=raw?JSON.parse(raw):{}}catch{}if(!response.ok)throw new Error(body.message||'Terjadi kesalahan pada server.');return body.data as T}
