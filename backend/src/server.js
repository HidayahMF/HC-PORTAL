const {createApp}=require('./app');const {env}=require('./config/env');const {validateAuthConfig:validateNomor}=require('./modules/legacy-nomor/config/auth');const {validateAuthConfig:validateKontrak}=require('./modules/legacy-kontrak/config/auth');const wagRuntime=require(require('node:path').resolve(__dirname,'modules/wag/runtime/server.js'));const app=createApp();const server=app.listen(env.port,async()=>{validateNomor();validateKontrak();console.log(`HC Portal backend listening on ${env.port}`);if(wagRuntime.initializeBackgroundServices) await wagRuntime.initializeBackgroundServices();});
async function shutdown(signal){console.log(`HC Portal shutdown: ${signal}`);try{await wagRuntime.shutdownBackgroundServices?.()}finally{server.close(()=>process.exit(0))}}process.on('SIGTERM',()=>shutdown('SIGTERM'));process.on('SIGINT',()=>shutdown('SIGINT'));
